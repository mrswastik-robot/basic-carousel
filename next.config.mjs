/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["akm-img-a-in.tosshub.com","www.apple.com", "www.channelnews.com.au"],
    }
};

export default nextConfig;
